require("../index.js");

describe('Specializer', function(){
  describe('#specialize()', function(){
    it('should set the ourl_version field in an item', function(){
      assert.equal(-1, [1,2,3].indexOf(5));
      assert.equal(-1, [1,2,3].indexOf(0));
    })
  })
})
